import styled from 'styled-components';

export const StyledFlashcards = styled.ul `
    display: flex;
    flex-direction: column;
    gap: 5px;

    margin-bottom: 100px;

    list-style-type: none;
`